{
  "name": "sourcecode",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/deploy.ps1",
    "preview": "vite preview",
    "deploy": "npm run build",
    "serve:root": "node ./scripts/local-server.mjs -r .. -H 127.0.0.1 -p 5500",
    "deploy:serve": "npm run build && powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/deploy.ps1"
  },
  "dependencies": {
    "aplayer": "^1.10.1",
    "buffer": "^6.0.3",
    "music-metadata-browser": "^2.5.11",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.7"
  }
}
